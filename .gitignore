.idea/
build/
install/
log/
*.log
__pycache__/
*.py[cod]
.pytest_cache/
# Unrelated separately-cloned tool plugin marketplace (untracked)
ECC/

# Run artifacts: dataflash and telemetry are too large for git (.BIN files
# routinely exceed GitHub's 100 MB hard limit).
logs/
*.BIN
*.tlog
*.tlog.raw
mav.parm
eeprom.bin
terrain/
telemetry_*.csv
week2_telemetry_*.csv

# Pose model weights: fetched by scripts/fetch_pose_model.sh (into ~/models,
# outside the repo), never tracked. Not a bare `models/` pattern: that also
# matched src/huitzilin_perception/models/, where Gazebo models are tracked.
*.onnx

# Per-user Claude Code settings.
.claude/
