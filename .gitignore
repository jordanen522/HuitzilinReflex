.idea/
build/
install/
log/
*.log
__pycache__/
*.py[cod]
.pytest_cache/
# Unrelated separately-cloned tool plugin marketplace (untracked)
ECC/

# Lab: raw dataflash and telemetry are too large for git (7.2 GB, five .BIN
# files over GitHub's 100 MB hard limit). The conclusions that rest on them
# are reproduced in lab/results/*.csv, which IS committed.
logs/
lab/logs/
*.BIN
*.tlog
*.tlog.raw
mav.parm
eeprom.bin
terrain/
week2_telemetry_*.csv
