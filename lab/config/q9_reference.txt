# q9_reference.txt — CONFIRMATION RUN FOR THE HEADLINE CLAIM.
# ~15 min, 1 cell, 30 throws. Run this BEFORE q7 and q8 if time is short.
#
# Not a new hypothesis. "10/10 at 20 m/s from a 26 m sensor" is one cell of ten
# throws, and a Wilson 95% lower bound on 10/10 is 0.72. Thirty throws in one
# session, at the SHIPPED dodge_speed_mps of 1.5, moves that to 0.88 with no
# between-cell seam to argue about. Rationale, the pre-registered >= 27/30, and
# the station-drift failure mode specific to a long cell are all in
# hz20_r26.0_n30.yaml -- read the _alt.csv before the save count.

TAG=ref30 RANGE=26.0 ORACLE_RATE=0.0 DODGE_SPEED=1.5 RESP_OUT=1 CONFIG=$LAB/config/hz20_r26.0_n30.yaml BATTERY_ARGS="-p hover_mode:=true" EXTRA_LAUNCH="oracle_params:=$LAB/params/oracle_stereo.yaml" EXPECT_BANNER="0.3, 0.02, 0.02"
