# udev rules for the Pi. Install to /etc/udev/rules.d/ (docs/HARDWARE.md).
#
# OAK-D Lite (Movidius, vendor 03e7): let a non-root user open it. Without this
# depthai-ros fails with an "insufficient permissions" X_LINK error.
SUBSYSTEM=="usb", ATTRS{idVendor}=="03e7", MODE="0666"
